const db = require("../db");
const {
  getAllEntry,
  getEntry,
  addEntry,
  updateEntry,
  deleteEntry,
} = require("../services/entryServices");

module.exports = {
  getAllEntry: (req, res) => {
    getAllEntry((err, results) => {
      if (err) {
        console.log(err);
        return;
      }
      console.log("results", results);
      return res.json(results);
    });
  },
};

module.exports = {
  getEntry: (req, res) => {
    getEntry((err, results) => {
      if (err) {
        console.log(err);
        return;
      }
      console.log("results", results);
      return res.json(results);
    });
  },
};

module.exports = {
  addEntry: (req, res) => {
    addEntry((err, results) => {
      if (err) {
        console.log(err);
        return;
      }
      console.log("results", results);
      return res.json(results);
    });
  },
};

module.exports = {
  updateEntry: (req, res) => {
    updateEntry((err, results) => {
      if (err) {
        console.log(err);
        return;
      }
      console.log("results", results);
      return res.json(results);
    });
  },
};
module.exports = {
  deleteEntry: (req, res) => {
    deleteEntry((err, results) => {
      if (err) {
        console.log(err);
        return;
      }
      console.log("results", results);
      return res.json(results);
    });
  },
};